#include <stdio.h>
#include <stdlib.h>

int main() {
    int values = 55, i = 0, minus;
    for (; i < values; i++)
    minus =+ i;
    values = values - 10;
    printf("\n");
    printf ("%i and number %i", minus, values);
 return 0;
}